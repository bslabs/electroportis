/* Empty stdafx.h to be used for non-MSVC compilers
 *
 * On MSVC the Windows-specific include directory (containing a real stdafx.h)
 * is listed first, and that stdafx.h is used instead of this one
 */
